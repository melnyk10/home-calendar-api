quarkus:
  application:
    name: home-calendar-api
  log:
    level: INFO
    min-level: ALL
    console:
      format: '%d{HH:mm:ss} %-5p traceId=%X{traceId}, spanId=%X{spanId} [%c{2.}] (%t) %s%e%n'
    category:
      "com.meln":
        level: INFO
